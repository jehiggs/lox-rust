fun fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}

fun count(n) {
  if (n < 2) return n;
  var a = count(n - 1);
  return a + 1;
}

var start = clock();
print fib(35);
print clock() - start;
